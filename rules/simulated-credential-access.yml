id: "DET-001"
title: "Simulated Credential Access Command"
status: "test"
description: "Detects the alpha sample credential access telemetry event."
severity: "P2-High"
logsource:
  product: "linux"
  category: "process_creation"
detection:
  selection:
    event_type: "process"
    command_contains: "credential access"
  condition: "selection"
tags:
  - "attack.credential_access"
  - "attack.t1003"

